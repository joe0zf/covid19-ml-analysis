# COVID ML ANALYSIS
## Requirements

* Python 3.13.7
* pip

## Installation

Clone the repository and navigate to the project directory:

```bash
git clone https://github.com/joe0zf/covid19-ml-analysis.git
cd covid19-ml-analysis
```

Create and activate a virtual environment:

```bash
python -m venv .venv
source .venv/bin/activate
```

On Windows:

```bash
python -m venv .venv
venv\Scripts\activate
```

Install the project dependencies:

```bash
pip install -r requirements.txt
```

## Project Structure

```text
covid19-ml-analysis/
├── data/
|   ├── processed/
|   ├── raw/
├── notebooks/
│   ├── preprocessing.ipynb
│   ├── supervised-learning.ipynb
│   └── unsupervised-learning.ipynb
├── models/
├── requirements.txt
└── README.md
```
